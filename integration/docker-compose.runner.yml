services:
  integration-runner:
    image: golang:1.26.6-bookworm
    working_dir: /workspace
    entrypoint: ["bash", "-c"]
    volumes:
      - ${INTEGRATION_REPO_ROOT}:/workspace
      - openfox-integration-gocache:/go-build-cache
      - openfox-integration-gomodcache:/go-mod-cache
    environment:
      GOCACHE: /go-build-cache
      GOMODCACHE: /go-mod-cache
      GOTOOLCHAIN: local
      CGO_ENABLED: "0"
      GOFLAGS: -tags=goolm,stdjson,integration

volumes:
  openfox-integration-gocache:
  openfox-integration-gomodcache:
